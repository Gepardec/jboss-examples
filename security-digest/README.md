Security with Digest
==============

Shows how to setup security in Wildfly/EAP with DIGEST algorithm.
Works also behind a reverse proxy, since it turns uri-validation off.
See 03_disable_uri_validation.cli
